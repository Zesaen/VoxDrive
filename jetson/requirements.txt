# VoxDrive Jetson 侧 Python 依赖
# 注意：Jetson 板上使用系统预装包（sentence-transformers 依赖 CUDA 版 torch，
# 板上 pip 重装会拉错 CPU 轮子）——本文件用于 PC 开发环境与板子重灌后的版本对照。
pyzmq>=25.0
numpy>=1.24
scikit-learn>=1.3
sentence-transformers>=2.2
PyQt5>=5.15
